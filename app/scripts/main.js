// Tabs swither
$('.tabs').on('click', '.tabs_buttons li', function(event) {
	event.preventDefault();
	/* Act on the event */
	var index = $(this).index();
	$('.tabs_blocks').
	console.log(index);
});